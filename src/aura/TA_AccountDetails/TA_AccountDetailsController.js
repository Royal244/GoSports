({
    onInit: function(cmp,event,helper){
        helper.doInit(cmp,event);
    },

    onOpenViewTab: function(cmp,event,helper){
        helper.doOpenViewTab(cmp,event);
    },

    onOpenEditTab: function(cmp,event,helper){
        helper.doOpenEditTab(cmp,event);
    },

    onClearAccount: function(cmp,event,helper){
        helper.doClearAccount(cmp,event);
    }
})