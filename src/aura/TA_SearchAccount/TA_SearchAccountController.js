({
    onSearchAccounts: function(component, event, helper){
        helper.doSearchAccounts(component,event);
    },

    onClear: function(component, event, helper){
        helper.doClear(component, event);
    },

    onStartEmpty: function(component, event, helper){
        helper.doStartEmpty(component, event);
    },
})